<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.absut.tasksapp.MainActivity">
    <item
        android:icon="@drawable/ic_sort_24dp"
        android:title="Sort"
        android:id="@+id/action_sort"
        app:showAsAction="always" >
        <menu>
            <item
                android:id="@+id/action_sort_by_due_date"
                android:title="Order by due date" />
            <item
                android:id="@+id/action_sort_by_date_created"
                android:title="Order by created date" />
        </menu>
    </item>

    <item
        android:id="@+id/action_delete_all"
        android:orderInCategory="100"
        android:title="Delete all completed"
        android:visible="false"
        app:showAsAction="never" />
</menu>